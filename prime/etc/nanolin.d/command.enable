#!/bin/sh

[ -z "$1" ] && echo "$NLHDR
	Enable a Nanolin command.

    Usage:

	${NLBIN##*/} enable <command>

    Available Commands:

$("$NLBIN" indent command list disabled)
" && exit

chmod +x "$NLDIR/${1##*/}"
