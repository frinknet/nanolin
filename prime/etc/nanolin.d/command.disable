#!/bin/sh

[ -z "$1" ] && echo "$NLHDR
	Disables a Nanolin command.

    Usage:

	${NLBIN##*/} disable <command>

    Available Commands:

$("$NLBIN" indent command list enabled)
" && exit

chmod -x "$NLDIR/${1##*/}"
