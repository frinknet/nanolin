#!/bin/sh

[ -z "$1" ] && echo "$NLHDR
	Disables a Nanolin command.

    Usage:

	$NLCLI ${NLACT/./ } <command>

    Available Commands:

$("$NLBIN" indent command list enabled)
" && exit

chmod -x "$NLDIR/${1##*/}"
